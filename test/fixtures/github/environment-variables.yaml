name: environment-variables

env:
  STRING: string
  NUMBER: 1

jobs:
  job1:
    name: Job 1
    env:
      STRING: string
      NUMBER: 1
    steps:
      - name: Step 1
        run: command line
        env:
          STRING: string
          NUMBER: 1
