pool:
  vmImage: ubuntu-latest
  name: Default
resources:
  repositories:
    - repository: templates
      type: git
      name: danielciuraru/dc-templates

extends:
  template: ./../testdata/pipelines/azure/templates/azure-partial-template.yaml
  parameters:
    trivyStep:
      - template: ./../testdata/pipelines/azure/templates/azure-trivy-template.yaml
